name: Hello World Test

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v6

      - name: 挨拶を出力
        run: echo "こんにちは、GitHub Actionsです"

      - name: 現在時刻を表示
        run: date

      - name: ファイル一覧を表示
        run: ls -la
